% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learning_tracks.R
\name{get_learning_tracks}
\alias{get_learning_tracks}
\title{Retrieve Learning Tracks}
\usage{
get_learning_tracks(skill_id = NULL, limit = NULL, offset = 0)
}
\arguments{
\item{skill_id}{Optional character string. If provided, filters for learning
tracks associated with this specific skill ID (e.g., \code{"skill_r"}).
Must be a single, non-empty string if specified. Defaults to \code{NULL} (no
skill filter, returns all tracks).}

\item{limit}{Optional integer. Maximum number of records to return.
Defaults to NULL (no limit).}

\item{offset}{Optional integer. Number of records to skip before returning results.
Useful for pagination. Defaults to 0.}
}
\value{
A \code{data.frame} containing columns \code{track_id}, \code{title}, \code{description},
and \code{url} for the retrieved learning tracks, ordered by \code{track_id}.
Returns an empty data frame if no tracks match the criteria.
}
\description{
Queries the \code{adem.learning_tracks} table to retrieve learning tracks,
optionally filtering by a required skill ID. Results are ordered by track ID.
}
\examples{
\dontrun{
# Get all learning tracks
all_tracks <- get_learning_tracks()

# Get learning tracks for R skills with pagination
r_tracks_page1 <- get_learning_tracks(skill_id = "skill_r", limit = 10)
r_tracks_page2 <- get_learning_tracks(skill_id = "skill_r", limit = 10, offset = 10)
}

}
\seealso{
\code{\link[=get_learning_track_by_id]{get_learning_track_by_id()}}, \code{\link[=get_skills]{get_skills()}}, \code{\link[=connect_db]{connect_db()}}
}
