% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skills.R
\name{get_skills}
\alias{get_skills}
\title{Retrieve Skills}
\usage{
get_skills(limit = 100)
}
\arguments{
\item{limit}{An integer specifying the maximum number of skills to return.
Must be a single positive integer. Defaults to 100 if not specified.
Function stops if \code{limit} is invalid.}
}
\value{
A \code{data.frame} containing the \code{skill_id} and \code{skill_label}
for the retrieved skills, ordered by \code{skill_id}. The number of rows
will be at most \code{limit}. Returns an empty data frame if no skills
are found (or if the table is empty). Stops with an error if the
database query fails or if the input \code{limit} is invalid.
}
\description{
Queries the \code{adem.skills} table to retrieve a list of skills,
ordered by their ID. Allows limiting the number of results returned.
}
\examples{
\dontrun{
# Assumes connect_db is defined and database credentials are set

# Get the default number of skills (up to 100)
skills_default <- get_skills()
print(head(skills_default))

# Get a specific number of skills
top_10_skills <- get_skills(limit = 10)
print(top_10_skills)

# Example of invalid input (will cause an error)
try(get_skills(limit = -5))
try(get_skills(limit = 0))
try(get_skills(limit = "ten"))
try(get_skills(limit = c(10, 20)))
}

}
\seealso{
\code{\link[=get_skill_by_id]{get_skill_by_id()}}, \code{\link[=connect_db]{connect_db()}}
}
