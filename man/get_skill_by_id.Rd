% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill_by_id.R
\name{get_skill_by_id}
\alias{get_skill_by_id}
\title{Retrieve a Skill by its ID}
\usage{
get_skill_by_id(skill_id)
}
\arguments{
\item{skill_id}{A character string representing the unique ID of the skill
to retrieve (e.g., \code{"skill_r"}). Must be a single, non-empty string.}
}
\value{
A \code{data.frame} containing one row with columns \code{skill_id} and
\code{skill_label} if the skill is found. Returns \code{NULL} if no skill matches
the provided \code{skill_id}. The function will stop with an error if the
database query fails or if the input \code{skill_id} is invalid.
}
\description{
Queries the \code{adem.skills} table for a specific skill based on its unique
identifier.
}
\examples{
\dontrun{
# Ensure connect_db() is defined and database credentials are set

# Example: Get the skill with ID "skill_r"
r_skill_details <- get_skill_by_id("skill_r")
if (!is.null(r_skill_details)) {
  print(r_skill_details)
} else {
  print("Skill 'skill_r' not found.")
}

# Example: Try to get a skill that likely doesn't exist
non_existent_skill <- get_skill_by_id("skill_imaginary_123")
print(non_existent_skill) # Should print NULL if not found

# Example of invalid input (will cause an error)
try(get_skill_by_id(123))
try(get_skill_by_id(c("a", "b")))
try(get_skill_by_id(""))
}

}
\seealso{
\code{\link[=get_skills]{get_skills()}}, \code{\link[=connect_db]{connect_db()}}
}
