% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/company_details.R
\name{get_company_details}
\alias{get_company_details}
\title{Retrieve Company Details and Vacancies}
\usage{
get_company_details(company_id)
}
\arguments{
\item{company_id}{A single integer representing the unique ID of the company
to retrieve details for. Must be a single, non-missing, integer value.}
}
\value{
A list containing two data frames:
\describe{
\item{\code{company}}{A 1-row data frame with columns \code{company_id}, \code{name},
and \code{sector} for the specified company.}
\item{\code{vacancies}}{A data frame (potentially with 0 rows) containing
columns \code{vacancy_id}, \code{occupation}, \code{canton}, \code{year}, \code{month} for all
vacancies associated with that company.}
}
Returns \code{NULL} if the \code{company_id} is not found in the \code{adem.companies}
table. Stops with an error if the database query fails or if the input
\code{company_id} is invalid.
}
\description{
Fetches information about a specific company from \code{adem.companies} and
all associated vacancies from \code{adem.vacancies} based on the company ID.
}
\examples{
\dontrun{
# Assumes connect_db is defined and database credentials are set

# Get details for company with ID 42 (replace with a valid ID)
details <- get_company_details(42)
if (!is.null(details)) {
  print("Company Info:")
  print(details$company)
  print("Vacancies:")
  print(head(details$vacancies))
} else {
  print("Company with ID 42 not found.")
}

# Try an ID that likely doesn't exist
non_existent <- get_company_details(999999)
print(non_existent) # Should print NULL

# Examples of invalid input (will cause errors)
try(get_company_details("abc"))
try(get_company_details(c(1, 2)))
try(get_company_details(10.5))
}

}
\seealso{
\code{\link[=get_companies]{get_companies()}}, \code{\link[=connect_db]{connect_db()}}
}
